#include<stdio.h>
#include<math.h>
#include<stdlib.h>

int main()
{
    int x, y;
    int sum;
    printf("Enter two numbers: ");
    scanf("%d%d", &x, &y);
    sum = x + y;
    printf("The sum is: %d", sum);
}